{% if title %}
  <h3>{{ title }}</h3>
{% endif %}
<div class="dcf-d-grid dcf-grid-cols-2 dcf-grid-cols-3@sm dcf-grid-cols-4@md dcf-grid-cols-5@xl dcf-col-gap-vw dcf-row-gap-0 dcf-mt-6">
  {% for row in rows %}
    {%
      set row_classes = [
        default_row_class ? 'views-row',
        'ucomm-grid-item',
      ]
    %}
    <div{{ row.attributes.addClass(row_classes) }}>
      {{- row.content -}}
    </div>
  {% endfor %}
</div>
